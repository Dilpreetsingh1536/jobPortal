<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta
            name="viewport"
            content="width=device-width, initial-scale=1, shrink-to-fit=no"
        />
        <title>Employer Dashboard</title>
        <link
            rel="stylesheet"
            href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        />
        <link rel="stylesheet" type="text/css" href="css/nav.css" />
        <link rel="stylesheet" type="text/css" href="css/footer.css" />
        <link rel="stylesheet" type="text/css" href="css/main.css" />
    </head>
    <%- include('./partials/nav.ejs') %>

    <body style="background-color: #f8f9fa">
        <div class="container mt-5">
            <h1 class="mb-4">Employer Dashboard</h1>
            <div class="row">
                <div class="col-md-6">
                    <div
                        class="card"
                        style="
                            border: 1px solid #ccc;
                            border-radius: 8px;
                            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
                            padding: 20px;
                            margin-bottom: 20px;
                        "
                    >
                        <div class="card-body">
                            <div class="logoContainer">
                                <img
                                    src="<%= employer.logo %>"
                                    alt="User Logo"
                                    class="logo"
                                    id="user-logo"
                                />
                            </div>
                            <div class="row">
                                <div class="col-md-4 mb-3">
                                    <a
                                        href="#"
                                        class="btn btn-primary btn-block"
                                    >
                                        <i class="fas fa-edit"></i> Edit
                                    </a>
                                </div>
                            </div>
                            <h3
                                class="card-title"
                                style="font-size: 24px; font-weight: bold"
                            >
                                Company: <%= employer.employerName %>
                            </h3>
                            <h5
                                class="card-subtitle mb-3 text-muted"
                                style="font-size: 18px"
                            >
                                ID: <%= employer.employerId %>
                            </h5>
                            <p
                                class="card-text"
                                style="font-size: 16px; margin-top: 10px"
                            >
                                Email: <%= employer.email %>
                            </p>
                            <a
                                href="/edit-emp-details"
                                class="btn btn-success"
                                style="margin-top: 20px"
                                >Edit Details</a
                            >
                            <a
                                href="/empChangePassword"
                                class="btn btn-info"
                                style="margin-top: 20px"
                                >Change Password</a
                            >
                        </div>
                    </div>
                </div>
                <div class="col-md-6">
                    <div
                        class="card"
                        style="
                            border: 1px solid #ccc;
                            border-radius: 8px;
                            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
                            padding: 20px;
                            margin-bottom: 20px;
                        "
                    >
                        <div class="card-body">
                            <h3
                                class="card-title"
                                style="font-size: 24px; font-weight: bold"
                            >
                                New Applications
                            </h3>
                        </div>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-6">
                    <div
                        class="card"
                        style="
                            border: 1px solid #ccc;
                            border-radius: 8px;
                            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
                            padding: 20px;
                            margin-bottom: 20px;
                        "
                    >
                        <div class="card-body">
                            <h3
                                class="card-title"
                                style="font-size: 24px; font-weight: bold"
                            >
                                Posted Jobs
                            </h3>

                            <% if (error && error.length > 0) { %>
                            <div class="alert alert-danger" role="alert">
                                <%= error %>
                            </div>
                            <% } %> <% if (success && success.length > 0) { %>
                            <div class="alert alert-success" role="alert">
                                <%= success %>
                            </div>
                            <% } %>

                            <form action="/setEditJobId" method="post">
                                <% if (jobs && jobs.length > 0) { %>
                                <ul class="list-group">
                                    <% jobs.forEach(job => { %>
                                    <li class="list-group-item">
                                        <h5><%= job.jobTitle %></h5>
                                        <p>
                                            <strong>Sector:</strong> <%=
                                            job.sector %>
                                        </p>
                                        <p>
                                            <strong>Salary:</strong> <%=
                                            job.salary %>
                                        </p>
                                        <p>
                                            <strong>Location:</strong> <%=
                                            job.street %>, <%= job.city %>, <%=
                                            job.province %> <%= job.postalCode
                                            %>
                                        </p>
                                        <p>
                                            <strong>Description:</strong> <%=
                                            job.description %>
                                        </p>
                                        <p>
                                            <strong>Job Status:</strong> <%=
                                            job.status %>
                                        </p>
                                        <button
                                            type="submit"
                                            class="btn btn-warning"
                                            name="jobId"
                                            value="<%= job._id %>"
                                        >
                                            Edit
                                        </button>

                                        <a
                                            href="/setDeleteJobId/<%= job._id %>"
                                            class="btn btn-danger"
                                            >Delete</a
                                        >
                                    </li>
                                    <% }); %>
                                </ul>
                                <% } else { %>
                                <p>No jobs posted yet.</p>
                                <% } %>
                            </form>

                            <a
                                href="/listJob"
                                class="btn btn-success"
                                style="margin-top: 20px"
                                >List a Job</a
                            >
                        </div>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="card" style="border: 1px solid #ccc; border-radius: 8px; box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); padding: 20px; margin-bottom: 20px;">
                        <div class="card-body">
                            <h3 class="card-title" style="font-size: 24px; font-weight: bold">New Messages</h3>
                            <% if (messages && messages.length > 0) { %>
                                <div class="list-group">
                                    <% messages.forEach(function(message) { %>
                                        <div class="list-group-item list-group-item-action">
                                            <div class="row">
                                                <div class="col-12">
                                                    <p class="mb-1"><strong>Message:</strong> <%= message.message %></p>
                                                </div>
                                            </div>
                                            <div class="row align-items-center">
                                                <div class="col-md-4">
                                                    <p class="mb-1"><strong>From:</strong> <%= message.adminUniqueId %></p>
                                                </div>
                                                <div class="col-md-4">
                                                    <small><strong>Sent:</strong> <%= message.createdAt.toDateString() %></small>
                                                </div>
                                                <div class="col-md-4 text-md-right">
                                                    <form action="/deleteMessageForEmployer/<%= message._id %>" method="POST" style="display: inline;">
                                                        <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                                                    </form>
                                                </div>
                                            </div>
                                        </div>
                                    <% }); %>
                                </div>
                            <% } else { %>
                                <p>No new messages.</p>
                            <% } %>
                            <a href="/userAllMessages" class="btn btn-info" style="margin-top: 5px">View More</a>
                            <h3
                                class="card-title"
                                style="font-size: 24px; font-weight: bold"
                            >
                                Messages
                            </h3>
                            <p class="card-text">
                                Total Messages:
                                <span class="badge badge-primary"
                                    ><%= messageCount %></span
                                >
                            </p>
                            <a href="/viewMsg" class="btn btn-primary"
                                >View Messages</a
                            >
                            <a href="/sentMsg" class="btn btn-primary"
                                >Sent Messages</a
                            >
                        </div>
                    </div>
                </div>
                
            </div>
        </div>

        <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    </body>
    <%- include('./partials/footer.ejs') %>
</html>
