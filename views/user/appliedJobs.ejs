<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <title>Liked Jobs</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" />
    <link rel="stylesheet" type="text/css" href="/css/nav.css" />
    <link rel="stylesheet" type="text/css" href="/css/footer.css" />
</head>
<body>
<%- include('./partials/nav.ejs') %>

<div class="container mt-5">
    <h1 class="mb-4">Applied Jobs</h1>
    <ul class="list-group">
        <% appliedJobs.forEach((appliedJob, index) => { %>
            <li class="list-group-item d-flex justify-content-between align-items-center">
                <span>
                    <strong><%= appliedJob.job.jobTitle %></strong> - <%= appliedJob.job.employerId.employerName %>
                </span>
                <div>
                  
                    <button type="button" class="btn btn-primary btn-sm" data-toggle="modal" data-target="#jobDetailModal<%= index %>">
                        View
                    </button>
                </div>
                <!-- Job Detail Modal -->
                <div class="modal fade" id="jobDetailModal<%= index %>" tabindex="-1" role="dialog" aria-labelledby="jobDetailModalLabel<%= index %>" aria-hidden="true">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title" id="jobDetailModalLabel<%= index %>">Job Details</h5>
                                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                    <span aria-hidden="true">&times;</span>
                                </button>
                            </div>
                            <div class="modal-body">
                                <p><strong>Job Title:</strong> <%= appliedJob.job.jobTitle %></p>
                                <p><strong>Employer:</strong> <%= appliedJob.job.employerId.employerName %></p>
                                <p><strong>Sector:</strong> <%= appliedJob.job.sector %></p>
                                <p><strong>Location:</strong> <%= appliedJob.job.city %>, <%= appliedJob.job.province %></p>
                                <p><strong>Salary:</strong> <%= appliedJob.job.salary %></p>
                                <p><strong>Street:</strong> <%= appliedJob.job.street %></p>
                                <p><strong>Decision:</strong> <%= appliedJob.decision %></p>
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                            </div>
                        </div>
                    </div>
                </div>
            </li>
        <% }); %>        
    </ul>
    <nav aria-label="Page navigation example" class="mt-4" style="background-color: white;">
        <ul class="pagination">
            <% for(let i = 1; i <= totalPages; i++) { %>
                <li class="page-item <%= i === currentPage ? 'active' : '' %>">
                    <a class="page-link" href="/appliedJobs?page=<%= i %>"><%= i %></a>
                </li>
            <% } %>
        </ul>
    </nav>    
</div>

<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>

<%- include('./partials/footer.ejs') %>
</body>
</html>
